/*
     名字：隱密之地
     地图：雅典娜禁地&amp;lt;休息室&gt;
     描述：920010400
 */

function act() {
    rm.getPlayer().getMap().broadcastMessage(Packages.tools.packet.MaplePacketCreator.serverNotice(6, "昔日的音樂再次響起，這是多麼熟悉的聲音"));
}
